{
  "$schema": "https://www.schemastore.org/package.json",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build",
    "dev": "vite",
    "prepare": "husky",
    "pre-commit-windows": "composer rector && php artisan ide-helper:generate && php artisan ide-helper:models -RW && php vendor/bin/pint && git add .",
    "pre-commit-mac": "composer rector && php artisan ide-helper:generate && php artisan ide-helper:models -RW && php ./vendor/bin/pint && git add ."
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.0.0",
    "axios": "^1.11.0",
    "concurrently": "^9.0.1",
    "laravel-vite-plugin": "^2.0.0",
    "tailwindcss": "^4.0.0",
    "vite": "^7.0.7"
  },
  "dependencies": {
    "@shufo/prettier-plugin-blade": "^1.16.1",
    "husky": "^9.1.7",
    "prettier": "^3.7.4"
  }
}
